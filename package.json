{
  "name": "etsy-ads-keyword-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension: export, AI-review, and safely disable Etsy Ads targeted keywords.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
